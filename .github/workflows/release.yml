name: release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write
  packages: write

jobs:
  publish:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest]
    
    steps:
      - uses: actions/checkout@v7
      - run: corepack enable
      - uses: actions/setup-node@v7
        with:
          node-version: lts/*
          cache: pnpm
        
      - name: 📦 Install dependencies
        run: pnpm install
        
      - name: ⚡ Build and compile
        run: node --run compile

      - name: 🏷️ Rename release asset
        run: mv pkg/jim-rtmp.exe pkg/jim-rtmp-${{ github.ref_name }}.exe

      - name: 📤 Publish assets
        uses: softprops/action-gh-release@v3
        with:
          draft: true
          files: pkg/*.exe
          fail_on_unmatched_files: true

      - name: 📝 Create release notes
        run: npx changelogen gh release --token ${{ secrets.GITHUB_TOKEN }}
